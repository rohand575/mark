export const WELCOME_MARKDOWN = `# Welcome to Mark

A quiet place for your thoughts. Everything you see is **directly editable** — just click anywhere and start typing.

## The basics

- Type \`/\` on an empty line to insert blocks — headings, lists, quotes, tables, code…
- Select text to format it: **bold**, *italic*, \`code\`, ~~strikethrough~~, links
- \`⌘S\` / \`Ctrl+S\` saves · \`⌘O\` opens a file · \`⌘N\` starts a new one

## Why Mark?

> The best writing tool is the one that disappears.

Mark renders your markdown beautifully *while* you write it. No split panes, no preview button — the document is the editor.

\`\`\`ts
// Even code feels at home here
const focus = (mind: string) => mind.trim();
\`\`\`

Double-click any \`.md\` file on your computer and it opens right here, in a new tab. Happy writing.
`;

export const UNTITLED_MARKDOWN = "";
